{
  "name": "@headless-form/example-vanilla",
  "version": "0.0.0",
  "private": true,
  "description": "Vanilla TS example — uses @headless-form/core directly",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@headless-form/core": "workspace:*",
    "marked": "^18.0.5"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.2",
    "vite": "^5.4.0"
  }
}
